# === API Backend ===
# Backend API URL (must match server PORT and FRONTEND_URL in server/.env)
REACT_APP_API_URL=http://localhost:5000

# === Geolocation ===
# The app uses browser Geolocation API to get user's current location
# No key needed — built-in to the browser
